# status

state: active
remote: github-public
updated: 2026-08-20
stale-after-days: 30

## kpi
None. The product is one markdown file (SKILL.md); everything else exists only to prove the
examples compile. A number would not describe it better than the README already does.
Declared deviation from the 1-3 KPI rule.

## now
Published as `nqwrc/suckless-harness`: a skill file that constrains an AI coding agent to
write C in the suckless.org style, with a test suite proving the examples compile, behave,
and build under `make`.

## backlog
- nothing tracked here; see the repo's open issues
